# Experiments

Each experiment is isolated from the core infrastructure.

An experiment may contain its own setup, tests, scripts, data, and results.

The core infrastructure should not need to understand the internal implementation of an experiment.