# Python

Python is supported as an experiment runtime.

Python is not part of the Testing Ground core by default.

Experiments that require Python should keep their Python-specific code and dependencies isolated from the core infrastructure.